[project]
name = "bptree"
version = "0.1.0"
description = "The Python environment for the Bptree project"

requires-python = ">=3.10,<4.0"
dependencies = [
    "python-dotenv >=1.1.0",
    "pre-commit >=4.2.0"
]
